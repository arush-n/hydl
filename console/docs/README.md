# Console documentation

This directory is the user-facing reference for the Console service. It covers
the API, architecture, and decision-rate choices.

Start with [`API.md`](API.md) for endpoints and [`ARCHITECTURE.md`](ARCHITECTURE.md)
for the service boundaries. The service entry point is
[`../server.py`](../server.py).
